# 2026-08-28

## 网站结构梳理
- 站点当前结构：根 `index.html`(主页) + `main.css` + `theme.js`(日出日落主题) + `bg.js`(新增动态背景)
- `Book/` 电子书阅读器（B 键入口），`Music/` 音乐播放器（M 键入口）
- 之前做的 `A.html` 书签页已不存在，被 Book 阅读器取代；git 分支 `site`，工作树干净
- 主页无 Book/Music 入口按钮，仅键盘快捷键可达

## 新增功能：动态交互粒子背景 (bg.js)
- 用户满意现状，想加「互动+实用+视觉」类功能，最终选「动态交互背景」
- 实现：canvas 全屏固定、z-index:-1 在内容之下不挡点击；粒子漂移+邻近连线（星座效果），鼠标/触摸附近连线互动
- 自动读 `<html class="dark">` 适配明暗（暗色亮星 #99A3BA / 亮色灰点）；prefers-reduced-motion 时跳过渲染；零依赖
- 已接入 index.html（</body> 前的 <script src="bg.js">）
- 验证：Edge headless dump-dom 确认 canvas 生成且属性正确，无 JS 报错
- 注意：Book/Music 页面尚未加此背景（保持范围聚焦主页）

## 18:15 给 Book/ 加背景 + 永久不碰 Music/ 约定
- Book/index.html 末尾加 `<script src="../bg.js"></script>`（用 `../` 指向根脚本），已验证 canvas 正常生成
- 用户明确指令：**永远不要再修改 Music/ 目录任何文件**，已记入 MEMORY.md 长期约定
- 未动 Music/，未改 Book 既有的 theme.js/main.css 路径（虽从子目录解析会 404，属既有状态，保持用户满意现状）

## 18:20 天气小组件 + ? 帮助面板（主页）
- 新增 `weather.js`：geojs 免费 IP 定位 → open-meteo 免费取当前天气（均无需 key/CORS 友好）；WMO 天气代码映射中文+emoji；按 W 显示/隐藏，加载完自动显示一次；失败回退北京→再失败提示「获取失败」
- 新增 `shortcuts.js`：按 ? 切换帮助面板，Esc/点击遮罩关闭
- index.html：<head> 加 <style>（天气卡 + 帮助卡，用 main.css 的 --bg/--text/--h1/--quote-line 主题变量，明暗自适应）；body 末加 #weather-widget / #shortcuts-help 两个 DOM + 引入 weather.js/shortcuts.js
- 帮助面板列出快捷键：B 阅读器 / M 音乐 / W 天气 / ? 帮助 / Esc 关闭
- 验证：node --check 两脚本通过；Edge dump-dom 确认两 DOM 与三脚本引用齐全
- 注意：weather.js 走外网 API，本地无网时会显示「获取失败」，部署后正常

## 18:24 贪吃蛇小游戏（用户要求：轻量、无图片资源）
- 用户从候选（贪吃蛇/2048/扫雷/记忆翻牌）中选「贪吃蛇」
- 新增 `snake.js`（canvas 画方块，零图片；方向键/WASD+空格暂停+Esc回主页+移动端滑动；读 .dark 适配明暗；最高分存 localStorage）+ `snake.html`（独立页，引入 theme.js/main.css 保证主题一致）
- 主页 `index.html`：inline keydown 加 `G`→snake.html；? 帮助面板新增 `G 贪吃蛇小游戏` 一行
- 验证：node --check snake.js 通过；Edge dump-dom 确认 #snake/#score/#over 与 snake.js 引用齐全
- 未碰 Music/（持续遵守约定）

## 18:29 把贪吃蛇移入 Game/ 目录
- 用户要求建 `Game/` 文件夹收纳小游戏。执行：普通 mv（文件未被 git 跟踪），将 `snake.html`/`snake.js` 移入 `Game/`
- 修正 Game/snake.html 内相对路径：icomoon.png/main.css/theme.js → `../`，返回主页链接 `index.html`→`../index.html`（snake.js 同目录，`src="snake.js"` 不变）
- 修正 Game/snake.js 内 Esc 回主页：`'index.html'`→`'../index.html'`
- 主页 index.html `G` 键：`'./snake.html'`→`'./Game/snake.html'`
- 验证：node --check Game/snake.js 通过；Edge dump-dom 确认 Game/snake.html 的 #snake/#score/#over 与 `../main.css`/`../theme.js`/`snake.js` 引用齐全；根目录无 snake 残留

## 18:32 天气组件改为默认隐藏
- 用户要求：天气不再自动显示，仅按 W 才显示
- 改动 weather.js：删除 `render()` 与 `fail()` 末尾的 `show()` 调用（原「数据就绪自动显示一次」逻辑），保留初始 `hide()` 与 W 键 toggle；数据仍在后台加载，按 W 即展示已就绪内容
- 验证：node --check 通过；grep 确认 show() 仅剩函数定义，无自动调用

## 18:36 工具箱 Tools/（采纳用户"听取建议"，先做4个零依赖工具）
- 新建 `Tools/tools.html`（标签切换：时间戳/密码/Base64/哈希，引用 ../main.css + ../theme.js 保证主题一致）+ `Tools/tools.js`（全部纯前端逻辑，零依赖）
- 工具清单：①时间戳（Unix秒/毫秒 ↔ 本地/UTC/ISO 双向同步，"使用当前时间"按钮）②密码生成（长度4-64、4类字符集、crypto.getRandomValues 随机、熵强度提示、复制）③Base64（编码/解码，UTF-8 安全，错误处理）④哈希（SHA-1/256/384/512，Web Crypto，MD5 原生不支持故未提供，已注释说明）
- 主页 index.html：inline keydown 加 `T`→./Tools/tools.html；? 帮助面板新增 `T 工具箱`
- 验证：node --check Tools/tools.js 通过；Edge dump-dom 确认 tabs/#ts-unix/#ts-datetime/#pw-out/#hash-out/#b64-in 与 ../main.css/../theme.js/../index.html/../icomoon.png 引用齐全；T 键已接
- 未碰 Music/（持续遵守约定）

## 18:40 工具箱再扩充 5 个工具（用户点名：文本/进制/单位/计算器/二维码）
- 工具箱现共 9 个标签：时间戳/密码/Base64/哈希/文本/进制/单位/计算器/二维码
- 新增 `Tools/qrcode.js`：下载 Kazuhiko Arase 的 qrcode-generator@1.4.4（MIT，56KB），本地离线、运行时生成 data-URI 图片，不落盘、不联网
- tools.html：加 5 个 tab + 5 个 panel + 对应 CSS；`<script src="qrcode.js">` 在 tools.js 之前
- tools.js 新增逻辑：①文本（统计字符/词/行 + 大写/小写/首字母大写/去空格/去重行/删空行/反转/正则替换）②进制（2/8/10/16 互转，源进制可选）③单位（长度/时间/温度/像素px↔rem，温度特殊公式处理，像素根字号可调，任意框输入联动其余）④科学计算器（自写 shunting-yard 求值器，支持 + - * / % ^、括号、sin/cos/tan/sqrt/ln/log/π/e，纯 Math 无 eval）⑤二维码（qrcode(0,ec) 生成，4 档纠错）
- 验证：node --check 两 JS 通过；计算器求值器用 Node 跑 10 个已知用例（含 log/ln 映射、负号优先级、^右结合）全部 PASS；Edge dump-dom 确认 5 个 panel、calc-display、qr-out、unit-cat、text-in 与 qrcode.js/tools.js 引用齐全
- 未碰 Music/（持续遵守约定）
